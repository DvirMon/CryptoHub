import { DialogHostDirective } from './dialog-host.directive';

describe('DialogHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogHostDirective();
    expect(directive).toBeTruthy();
  });
});
